# Example 08 — Shawshank PAC (Cell Block C)

```bash
cargo test -p example-08-shawshank-pac
cargo run -p example-08-shawshank-pac -- --headless
cargo run -p example-08-shawshank-pac
```

Keys: `1` look Red · `N` next day · `2` look Andy · `3` talk · `4` loose stone · Esc quit.

See `NCP_MAPPING.md`. Content stills live in `examples/06-shawshank-pac/assets/`.
